name: NEXUS Core CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11' 
        
    - name: ⚙️ Install Dependencies (The NEXUS Stack)
      run: |
        python -m pip install --upgrade pip
        pip install numpy pandas river scikit-learn matplotlib seaborn tqdm psutil pytest typing_extensions
        
    - name: 🛠️ Set Python Path for Module Discovery (Still needed for tests folder)
      run: |
        # เพื่อให้ pytest ใน tests/ หา nexus_core.py ใน root ได้
        echo "PYTHONPATH=$GITHUB_WORKSPACE:$PYTHONPATH" >> $GITHUB_ENV
        
    - name: 🧪 Run Unit Tests (Validate ABSOLUTE FLAWLESS State)
      run: |
        # *********** เปลี่ยน: ให้ tests/test_nexus.py import จาก nexus_core ***********
        pytest tests/test_nexus.py
        
    - name: 🥇 Run NEXUS Benchmark (Prove SOTA Performance)
      run: |
        # *********** เปลี่ยน: รันไฟล์ nexus_core.py ***********
        python nexus_core.py
        
    - name: 📂 Upload Results Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nexus-benchmark-results-${{ github.sha }}
        path: results/ 
        if-no-files-found: error
        retention-days: 7
