name: NEXUS Core CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4

    - name: Cache pip packages
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: ⚙️ Install System Dependencies (Hardened Build)
      # ติดตั้งแพ็คเกจพื้นฐานที่จำเป็นสำหรับการคอมไพล์ SciPy/NumPy 
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential gfortran libblas-dev liblapack-dev python3-dev libopenblas-dev

    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: ⚙️ Install Python Dependencies (Using requirements.txt versions)
      # ติดตั้ง dependencies ทั้งหมด รวมถึง River>=0.18.0
      run: |
        python -m pip install --upgrade pip
        pip install "numpy>=1.26.0" "pandas>=2.1.0" "matplotlib>=3.8.0" "seaborn>=0.13.2" "scipy>=1.12.0" "psutil>=5.9.0" "tqdm>=4.66.0" "typing-extensions>=4.9.0" "pytest>=8.4.0" "river>=0.18.0"

    - name: 🛠️ Set Python Path for Module Discovery
      # สำคัญ: ทำให้ Pytest และ Python สามารถหา nexus_core.py ได้
      run: |
        echo "PYTHONPATH=$GITHUB_WORKSPACE:$PYTHONPATH" >> $GITHUB_ENV

    - name: 🧪 Run Unit Tests
      # ใช้ PYTHONPATH ที่ถูกตั้งค่าไว้ใน $GITHUB_ENV 
      run: |
        pytest tests/test_nexus.py

    - name: 🥇 Run NEXUS Benchmark
      run: |
        python nexus_core.py

    - name: 📂 Upload Results Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nexus-benchmark-results-${{ github.sha }}
        path: results/
        # เปลี่ยนเป็น warn เพื่อป้องกัน CI fail หากไม่มีไฟล์ผลลัพธ์บางส่วน
        if-no-files-found: warn
        retention-days: 7
