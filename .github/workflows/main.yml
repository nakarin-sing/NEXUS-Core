name: NEXUS Core CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11' 
        
    - name: ⚙️ Install Dependencies (The NEXUS Stack)
      run: |
        python -m pip install --upgrade pip
        
        # 1. ติดตั้ง NumPy เวอร์ชันที่เสถียร (1.24.4)
        pip install 'numpy==1.24.4'
        
        # 2. ติดตั้ง Dependencies ที่เหลือ พร้อมคำสั่งบังคับ Build ใหม่ทั้งหมด
        # --no-binary :all: บังคับให้ River ถูก Compile ใหม่ด้วย NumPy 1.24.4 ที่ถูกต้อง
        # --no-cache-dir ป้องกันการนำ Library เก่าที่ Corrupt มาใช้ซ้ำ
        pip install pandas 'river==0.19.0' scikit-learn matplotlib seaborn tqdm psutil pytest typing_extensions --no-cache-dir --no-binary :all:
        
    - name: 🛠️ Set Python Path for Module Discovery
      run: |
        # เพื่อให้ Pytest ใน tests/ หา nexus_core.py ใน root ได้
        echo "PYTHONPATH=$GITHUB_WORKSPACE:$PYTHONPATH" >> $GITHUB_ENV
        
    - name: 🧪 Run Unit Tests (Validate ABSOLUTE FLAWLESS State)
      run: |
        # รัน Pytest โดยคาดว่า tests/test_nexus.py และ nexus_core.py ใช้ river.proba
        pytest tests/test_nexus.py
        
    - name: 🥇 Run NEXUS Benchmark (Prove SOTA Performance)
      run: |
        # รันไฟล์หลักที่รวมโค้ดทั้งหมดแล้ว
        python nexus_core.py
        
    - name: 📂 Upload Results Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nexus-benchmark-results-${{ github.sha }}
        path: results/ 
        if-no-files-found: error
        retention-days: 7
